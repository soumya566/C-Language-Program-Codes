#include <stdio.h>
#include <string.h>
int main() {
 char pass[20];
 while(1) {
 printf("Enter password: ");
 scanf("%s", pass);
 if(strcmp(pass, "admin123") == 0) {
 printf("Valid Password\n");
 break;
 } else
 printf("Invalid\n");
 }
 return 0;
}
